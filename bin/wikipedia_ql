#!/usr/bin/env python
import glob
import sys
import os

# Always use local version, not the one installed on a system
lib = os.path.normpath(os.path.join(os.path.realpath(__file__), "../.."))
sys.path.insert(0, lib)

if __name__ == '__main__':
    import wikipedia_ql

    wikipedia_ql.run()

